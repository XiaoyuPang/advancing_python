(0).import导入

	使用sys模块，sys.path查看Python导入模块的路径
	避免导入模块的循环导入错误

(1). ==和is 
	Python里一切皆对象，对象有三个属性：type（对象类型）、id（对象地址）和value（对象的值）
	
	==，验证两个数据的“value和type”是否相等
	is，验证两个数据是否指向同一个id

(2).深拷贝和浅拷贝

	浅拷贝（copy.copy)：
		对数据是可变的还是不可变的，有不同的处理方式：如列表和元组
	
	深拷贝（copy.deepcopy):
		对被拷贝对象的完全复制，另起一个内存空间

(3).私有化

	了解 _x , __x , __x__, x_的区别

(4).property（属性）-----》
	理解property的两种用法，特别是装饰器@的用法（装饰器的使用场景？）
